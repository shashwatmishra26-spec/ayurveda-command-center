{
  "name": "ayurveda-command-center",
  "version": "1.0.0",
  "private": true,
  "description": "AI Command Center dashboard prototype for an Ayurvedic clinic — patient management, WhatsApp AI agent, Ayurveda knowledge copilot, appointments, inventory, order/shipment tracking and business intelligence in one screen.",
  "scripts": {
    "build": "bash build.sh",
    "build:standalone": "bash build.sh && node assemble.js",
    "build:react": "esbuild src/react-entry.js --bundle --format=iife --platform=browser --minify --outfile=dist/react-globals.bundle.js"
  },
  "dependencies": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "esbuild": "^0.27.7"
  }
}
